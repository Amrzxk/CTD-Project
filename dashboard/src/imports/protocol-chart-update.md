Improve the design of the "Protocol Distribution" chart to make it more visually consistent and professional for the cybersecurity dashboard.

1. Use a Single Color for All Bars

Replace the current multi-color bars (green, blue, pink) with a single consistent accent color.

Use a cybersecurity-style neon green color that matches the platform theme.

Suggested color:
#00FFA6

All protocol bars (TCP, UDP, ICMP) should use the same color.

The differences between the protocols should only appear through bar height, not color.

2. Chart Professional Styling

Ensure the bars have:

soft rounded corners
consistent spacing
clean grid lines

Avoid using multiple bright colors.

The chart should feel minimal, modern, and professional.

3. Tooltip Improvements

Fix the tooltip text formatting and spacing.

Current issue:
Tooltip text alignment and spacing look inconsistent.

Format the tooltip content like this:

UDP
Packets: 14

Use:

dark blue tooltip background
white text
soft border glow

Make the tooltip visually consistent with the dashboard card design.

4. Dashboard Theme Consistency

Ensure the chart styling matches the rest of the platform including:

card background
grid line colors
axis styling
font style

The final chart should feel clean, minimal, and aligned with a professional cybersecurity dashboard UI.
